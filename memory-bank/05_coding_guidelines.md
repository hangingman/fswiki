# コーディングガイドライン

- 既存FSWikiの責務とAPI契約を参照し、実装はRakuの型、Role、Callableに合わせる。
- Core、HTTP、Storageの責務を混ぜない。
- API公開は明示的な登録情報に限定する。
- Pluginは可能な限りデータ結果を返し、HTML/JSONの変換をRendererへ委譲する。
- 既存HTML出力を壊さない。
- 入力検証、権限検証、HTMLエスケープを省略しない。
- 変更範囲を必要最小限にする。
- 新規依存を追加する前に標準機能と既存依存を確認する。
- コメントは実装の理由と制約を記録する場合だけ追加する。
